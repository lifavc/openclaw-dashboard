# OpenClaw Dashboard Configuration
# Copy this file to .env.local and fill in your values

# Gateway connection is configured in the dashboard UI (Settings page)
# These env vars are optional overrides:

# Default gateway WebSocket URL (can be overridden in UI)
# NEXT_PUBLIC_GATEWAY_URL=wss://gateway.example.com

# Default gateway auth token (can be overridden in UI)
# NEXT_PUBLIC_GATEWAY_TOKEN=your-token-here

# Cloudflare Tunnel token (required for production)
# Create at: https://one.dash.cloudflare.com → Networks → Tunnels → Create
# Set the tunnel's public hostname to route to http://openclaw-dashboard:3000
# TUNNEL_TOKEN=your-tunnel-token-here
